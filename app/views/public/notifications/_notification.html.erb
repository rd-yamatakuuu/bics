<% visitor = notification.visitor %>
<% idea = notification.idea %>

<div>
  <%= link_to profile_user_path(notification.visitor) do %>
    <%= attachment_image_tag visitor, :profile_image, :fill,100,100, format: "jpeg", fallback: "no_image.png", size: "50x50", class:"profile-img-circle" %>
  <% end %>
  <%= notification.notification_form(notification) %><span><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
</div>